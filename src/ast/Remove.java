package ast;

public class Remove implements Mutate{

	@Override
	public boolean equals(Mutation m) {
		return this.getClass() == m.getClass();
	}

	@Override
	public void mutate(Node n) {
		if(n instanceof )
		
	}

}
